name: ecocity_mobile
description: EcoCity Mobile
publish_to: "none"
environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  go_router:
  dio:
  flutter_secure_storage:

flutter:
  uses-material-design: true
